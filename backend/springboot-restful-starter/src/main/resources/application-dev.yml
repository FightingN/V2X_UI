# 开发环境配置文件

server:
  port: 8081     # Springboot占用端口
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zhangxiang?serverTimezone=Asia/Shanghai&characterEncoding=utf8&useUnicode=true&useSSL=false
    username: root
    password:
  batch:
    initialize-schema=ALWAYS:
  devtools:
    restart:
      enabled: true     # 开启热更新